include ':app'

dependencyResolutionManagement {
    repositories {
        maven {
            url 'localRepo'
            metadataSources {
                mavenPom()
                artifact()
            }
        }
    }
}
