include 'app'

file('modules').listFiles().each { File moduleDir ->
    includeBuild moduleDir
}

dependencyResolutionManagement {
    repositories {
        ivy {
            name 'localrepo'
            url file("../../local-repo")
        }
    }
}

