apply plugin: 'com.android.application'

android {
    namespace "com.example.app"
    compileSdkVersion libs.versions.latestCompileSdk.get().toInteger()
    buildToolsVersion = libs.versions.buildToolsVersion.get()


    dynamicFeatures = [':featureA', ':featureB']

    defaultConfig {
        applicationId "android.databinding.instantappwithfeatures"
        minSdkVersion 23
    }

    buildFeatures {
        dataBinding = true
    }
}

dependencies {
    implementation "android.arch.core:runtime:1.1.0"
    implementation "com.android.support:animated-vector-drawable:${libs.versions.supportLibVersion.get()}"
    implementation "com.android.support:appcompat-v7:${libs.versions.supportLibVersion.get()}"

    implementation 'com.android.support.constraint:constraint-layout:' + libs.versions.constraintLayoutVersion.get()
}
